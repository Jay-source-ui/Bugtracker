spring.application.name=bugtracker

# Database Configuration - REMOVED duplicate and incorrect entries
# For Railway deployment (use environment variables)
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/bugtracker_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:JAYson@02}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration - REMOVED deprecated dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Server - Use Railway's PORT
server.port=${PORT:8080}

# Email Configuration (Optional - can remove for deployment)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jaysons974@gmail.com
spring.mail.password=kqdh bfkf gtec shgn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=/tmp/uploads